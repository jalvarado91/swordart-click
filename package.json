{
  "name": "swordart-click",
  "private": true,
  "scripts": {
    "build": "bun build src/main.ts --outdir dist --entry-naming 'game.[ext]' --target browser",
    "watch": "bun build src/main.ts --outdir dist --entry-naming 'game.[ext]' --target browser --watch"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
